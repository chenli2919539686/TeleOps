# TeleOps 面试 Demo 录屏分镜（2–3 分钟）

> 录屏工具：Windows 用 `Win+G`（Xbox 游戏栏）或 OBS；Mac 用 QuickTime。
> 录前先在本机起主界面（FastAPI 同源托管 `web/` 前端），浏览器开 **http://localhost:8000**。
> 建议边点边讲，语速放慢，每个界面停留 20–30 秒。
> 版本基线：v0.7.2+（工具复用 / 活视图）；录前若工具库已有 `temperature_probe`，先按「准备」重置，否则告警会走「♻️ 工具已存在，直接复用」分支，看不到造工具环节。

---

## 0. 准备（不录进视频）

```bash
cd TeleOps
python scripts/gen_data.py                 # 重置工具库到基础 2 项（清掉测试期自动生成件，保证缺口可复现）
python -m uvicorn src.api.server:app --reload --port 8000
# 浏览器打开 http://localhost:8000
```

确认左/右上 LLM 模式显示（mock 即可，无需 Key）。若只想展示「复用」不展示「造工具」，可跳过重置。

---

## 1. 开头（15 秒）
- 画面：主界面「Agent 作战室」（左侧导航：作战室 / 闭环看板 / 消息栏 / 接入层 / 拓扑视图 / 工具库 / 知识库）。
- 口播：「这是一个运营商云网的智能运维 Agent 平台。核心是『研发数字员工 × 运维 Agent』的纵向闭环：运维发现缺工具 → 研发自动造工具 → 运维复用。下面我跑给你看。」

---

## 2. 运维 Agent · 告警根因（45 秒）
- 作战室点一张 🛰️ **运维 Agent** 卡片 → 右侧打开 **Agent 工作台**（演示时可顺手把抽屉左缘往外拖宽）。
- 工作台左侧点预设样例 **「🌡️ 温度过热 host-1」**（或展开 **📚 真实机群告警样本** 选一条 🚨 严重告警，55 条 BlueGene/L 真实事件可搜可筛）。
- 点 **「▶ 分析告警」**。
- 口播：「运维 Agent 先做降噪，再用 CMDB 拓扑 + 知识库做根因推理，给出 Top-k 假设、置信度和证据。注意它说『缺 temperature_probe 工具』——这正是闭环的入口。」

---

## 3. 闭环看板 · 需求流转（60 秒，重点）
- 在分析结果底部点 **「登记并派发研发」**。
- 口播：「这是最有意思的地方。需求登记后自动派发给研发 Agent，状态从 pending 进 in_progress。」
- 切到 **🔄 闭环看板** 指给面试官看：需求卡片进入研发处理（busy）→ 自动生成工具脚本、注册进工具库、沉淀 SOP → 状态变 **tool_ready**。
- 切到 **📦 工具库**：库里多出 `temperature_probe`（活视图，全局 Agent 即时可见）。
- 回到工作台对同一条告警再点「▶ 分析告警」→ 口播：「注意，现在不再报缺工具了——它直接复用已有工具，**不会产生重复需求**。工具一经造出，全队复用。这就是闭环真正闭环了。」

> 若登记后想看状态走完 `done`：工作台复诊通过后再派发或让消息栏回流，看板需求即转 done，作战室卡片下方的「当前闭环」同步变绿。

---

## 4. 研发 Agent · 造工具（30 秒）
- 回到作战室点一张 🛠️ **研发 Agent** 卡片 → 工作台左侧「① 研发反馈单」填一句「需要探测 ONU 光功率、判定弱光根因的工具」。
- 点 **「🛠️ 研发造工具」** → 右侧显示生成过程与产物。
- 口播：「这里单独演示研发侧。给它一句话反馈，它就产出可运行的工具脚本、自动注册进工具库、并写一份处置 SOP 进知识库——这是数字员工平台化的体现。」

---

## 5. 收尾（15 秒）
- 口播：「整套东西零依赖、可离线演示：无 API Key 走离线 Mock，配置 Key 后无缝切国产大模型（DeepSeek / Qwen / GLM）。代码和在线 Demo 都在 GitHub，欢迎现场查看。」
- 画面停在 GitHub 链接 / 项目 README。

---

## 备用：若面试官想看接口
主界面同源端口即提供 Swagger：
```bash
# 主界面已起（uvicorn 8000）时直接打开：
# http://localhost:8000/docs   —— Swagger 交互文档
# http://localhost:8000/alerts —— 55 条真实告警样本 JSON
curl http://127.0.0.1:8000/health
curl -X POST http://127.0.0.1:8000/closed-loop/run -H "Content-Type: application/json" -d "{}"
```
