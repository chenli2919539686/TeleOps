# TeleOps 方法论验收文档（Methodology & Acceptance）

> 用途：面向「电信运营商运维 / 研发岗」求职作品验收与面试讲述。本文把项目从**工程治理**角度收敛成一份可照着讲的材料——不堆功能列表，而讲「为什么这么决策、谁负责、有什么风险、数据怎么落地、怎么保证质量、怎么验收」。
> 关联文档：`docs/04` 能力对照与优化路线 · `docs/06` 多副本部署实战验证 · `docs/10` 百人规模生产部署清单 · `docs/11` 合规与数据主权方案 · `docs/12` 一键演示与走查。
> 版本基线：v0.8.49（HEAD `fad0919`）。

---

## 0. 一句话定位

TeleOps 是一个**面向百人规模电信内部团队**的「运维告警 RCA 作战室」Agent 平台：告警进来 → 运维 Agent 给根因假设 → 发现缺工具 → 研发 Agent 现场造工具并复用 → 全程审计可回放 → 高危动作走人工审批（HITL）。它是**单进程即可演示、切配置即可上生产**的渐进式架构，不是玩具 Demo，也不是为炫技而过度设计的分布式系统。

---

## 1. ADR 架构决策记录（Architecture Decision Records）

> 原则：每个关键决策都记录「背景 / 决策 / 后果 / 备选」，方便评审人快速判断设计成熟度。

### ADR-001 技术栈：FastAPI + LangGraph + 原生 JS 前端 + 单进程
- **背景**：求职作品需在一个月内可演示，且要让面试官看清全链路，而非被脚手架淹没。
- **决策**：后端 FastAPI（异步、易测）；Agent 编排用 LangGraph（状态图可控、可暂停/恢复，契合 HITL）；前端用原生 JS + 静态文件（零构建，启动即见）；单进程 uvicorn 起服务。
- **后果**：✅ 零构建心智负担、改动即时可见、测试入口简单；⚠️ 单进程需靠「状态外部化开关」才能横向扩展（见 ADR-003）。
- **备选**：React+Vite（更现代但引入构建链与状态管理复杂度）；Django（重、异步弱）。

### ADR-002 多租户：A 档（单机单进程 + 注册建个人域 + 接口过滤）
- **背景**：百人团队需要租户隔离，但 K8s 命名空间级隔离对求职作品是过度工程。
- **决策**：注册自动建个人域 `ws-<用户名>` 并复制默认 4 个 Agent；`/workspaces`、`/agents` 按 `owner_id` 过滤；公共域 `core-net`（`owner_id=NULL`）所有人可见；**写隔离**经 `is_writable_by()` / `is_visible_to()` 服务端唯一判定入口。
- **后果**：✅ 零额外运维、隔离逻辑集中可测；⚠️ 单机承载，需水平扩展时配合 ADR-003。
- **备选**：B 档（每租户独立库）——隔离更彻底但运维与演示成本陡增，求职阶段不划算。

### ADR-003 状态外部化：统一开关 `TELEOPS_STATE_STORE=local|redis`
- **背景**：多副本下「A 副本发起的任务、B 副本看不到」会导致前端状态灯永远转圈。
- **决策**：把四类进程内状态（限流窗口 / 异步任务 / 告警流状态 / 并发信号量）抽象为 `Store` 接口，默认 `local`（零行为变更），设 `redis` 即外移；四类共用同一开关，避免配置分裂。
- **后果**：✅ 默认零变更、一键切 Redis 即「副本无状态」；⚠️ 播放面（RQ worker）需 Redis≥4.0 + Unix `fork()`，Windows 起不来（见 ADR-004）。
- **备选**：Celery 全量接管——功能强但依赖重、与现有线程执行器割裂。

### ADR-004 告警流执行器：默认线程、队列给 Linux
- **背景**：告警流是长生命周期线程，无法直接放进 Redis；且 Windows 无 `os.fork`。
- **决策**：`TELEOPS_STREAM_EXECUTOR=thread|queue`。默认 `thread`（demo 零变化）；`queue` 走 RQ，给 Linux/WSL2 部署。剧本不进 Redis（确定性 seed=42 重建），共享状态只放「播到第几条」。
- **后果**：✅ demo 与生产线一致；⚠️ Windows 只能验证状态/控制面跨副本，逐条播放需 Linux worker。

### ADR-005 HITL 两层闸 + 审批态外部化
- **背景**：企业级护城河是「高风险动作可控」，但闸写错位置会变死代码（曾写在 `server.agent_build` 死代码里）。
- **决策**：① 构建期全局闸（`TELEOPS_REQUIRE_APPROVAL` 或 admin 运行时开关）作用于 `tool.build` + `stream.start`；② 运行时按工具风险二次确认（`ToolRegistry.require_human_approval` 列表，如 `restart_service`）。审批单与运行时设置经 `TELEOPS_STATE_STORE=redis` 跨副本共享（fail-closed）。
- **后果**：✅ 开关真生效、多副本审批态一致；⚠️ 审批态默认落本地 JSON，多副本投产才需接 Redis。

### ADR-006 合规红线：未做「数据不出域 + 模型私有化」前不接真实告警/拓扑/日志
- **背景**：默认 LLM 是 DeepSeek 公有云，真实告警/拓扑送出去即违规。
- **决策**：硬红线——真实数据源接入前置条件 = 数据不出域（私有网络）+ 模型私有化（`TELEOPS_LLM_BASE_URL` 指向本地 OpenAI 兼容推理，**零代码改动**）。
- **后果**：✅ 默认演示只用合成 5G 样本，合规零风险；⚠️ 真实数据闭环被「拿不到私有化环境」阻塞（见风险 R1）。

### ADR-007 配置驱动 + mock 兜底（零依赖可演示）
- **背景**：面试官机器多半无 DeepSeek Key、无 Prometheus、无 OSS 桶。
- **决策**：LLM / 监控适配器（Prometheus/Loki）/ OSS / OIDC 全部「配置驱动 + 未配置自动回退 mock/demo」，保证任何环境一条命令跑通。
- **后果**：✅ 演示确定性极高；⚠️ 需在每个适配层维护 mock 与真实两条路径。

### ADR-008 数据源抽象：5G 数据集 loader 方向感知 + 列别名映射零代码切源
- **背景**：真实 OSS（华为/中兴/爱立信）列名千差万别，loader 写死列名就无法复用。
- **决策**：`FiveGKpiAdapter` 内置 `_COLUMN_ALIASES` + 配置 `column_aliases` 覆盖；`iter_rows`/`load_payloads` 透传别名；指标按方向（RSRP/RSRQ 越低越差）分别定阈值，payload 自带 severity。
- **后果**：✅ 任意厂商 OSS 填 `dataset_path`+`column_aliases` 即切源，零代码；⚠️ 仍卡真实导出文件（R1）。

### ADR-009 DB 方言翻译层：SQLite 默认 / Postgres 可切
- **背景**：演示要轻，生产要稳。
- **决策**：`TELEOPS_DB_DSN=postgresql://...` 即切，默认仍 SQLite；`db.py` 加 `_translate_sqlite_to_pg`（AUTOINCREMENT→IDENTITY、占位符 ?→%s、executescript 拆句）。
- **后果**：✅ 一套代码双后端；⚠️ PG 需额外驱动与迁移纪律。

### ADR-010 RBAC 引擎接线：路由层 `assert_perm` 正交叠加租户闸
- **背景**：RBAC 引擎 `enforce` 早已写好却是死代码（路由层零调用）。
- **决策**：`src/api/deps.py::assert_perm` 为路由层唯一权限判定入口，在租户闸之上叠加能力闸（`stream_start`→`tool.exec`、`build_agent`→`agent.manage`）；`/admin/roles` 可运营角色（`org.manage` 闸 + 两道锁死闸）。
- **后果**：✅ 越权写→403、越权读→404；✅ 不放松既有 admin-only 闸。

### ADR-011 故障域：LLM 端点级熔断（closed / open / half_open）
- **背景**：LLM 端点不可达时，原逻辑只是「调用失败→回退 Mock」，但下一条告警仍会再打一次已死端点——每条都被 30s 超时拖住，流水线看起来像"卡死"，`/stream/stop` 也要等 join 超时。
- **决策**：新增 `src/core/circuit_breaker.py`。连续失败达 `TELEOPS_LLM_CB_THRESHOLD`（默认 5）→ 打开熔断，窗口 `TELEOPS_LLM_CB_RESET`（默认 60s）内 **fail-fast 直接走 Mock**；窗口满后放行一次探测（half_open），成功即闭合、失败重新打开。状态经 `/health` 的 `llm_breaker` 字段与 `/metrics` 的 `teleops_llm_breaker_state` gauge 暴露。`TELEOPS_LLM_CB_ENABLED=0` 时完全惰性（与改造前逐字节一致）。
- **后果**：✅ 端点故障不再拖垮整条流水线、可观测可运维；⚠️ 熔断期间诊断走 Mock，需运维从 `/health` 发现并恢复端点。

---

## 2. RACI 责任分配矩阵

角色（来自 `src/core/db.py` `BUILTIN_ROLES`）：`super_admin`(全权) / `org_admin` / `sre` / `dev` / `viewer`；新注册用户默认角色 `sre`。外加「平台/系统」列表示基础设施责任方。

| 活动 | super_admin | org_admin | sre | dev | viewer | 平台/系统 |
|---|---|---|---|---|---|---|
| 账号注册 / 登录（自服务） | A | I | I | R | R | R(执行) |
| 邀请码开关（`TELEOPS_INVITE_CODE`） | A/R | R | I | I | I | — |
| 组织节点 / 成员 / 角色管理（`org.manage`） | A | R | I | I | I | — |
| 业务域 CRUD（`ws.manage`，公共域仅 admin 可写） | A | R | C | I | I | — |
| 域内 Agent 增删改（`agent.manage`） | A | C | R | R | I | — |
| 告警确认 / 启动告警流（`alert.ack`） | A | C | R | I | I | — |
| 工具执行 / 造工具（`tool.exec`） | A | C | R | I | I | — |
| 高危动作人工审批（HITL） | A/R | R | C(提请) | C(提请) | I | — |
| 审计查看（`audit.view`） | A | R | R | I | R | — |
| 审批单批准 / 拒绝 | A/R | R | I | I | I | — |
| 部署 / 扩容 / Redis / Caddy | A | C | C | I | I | R(执行) |
| 合规与数据主权决策（红线） | A | C | I | I | I | C |

图例：R=负责执行 · A=批准/问责 · C=被咨询 · I=知会。

---

## 3. 风险登记册（Risk Register）

评级用 高/中/低。缓解措施均已在代码中落地或文档化。

| # | 风险 | 类别 | 可能性 | 影响 | 缓解措施 | 残余风险 |
|---|---|---|---|---|---|---|
| R1 | 真实告警/拓扑/日志送公有云导致数据出域违规 | 合规 | 中 | 高 | ADR-006 红线 + 模型私有化开关（`TELEOPS_LLM_BASE_URL`）；默认仅合成样本 | 中（需采购私有化推理） |
| R2 | 多副本状态不一致（默认 local 不跨副本） | 架构 | 中 | 高 | `TELEOPS_STATE_STORE=redis` 实战验证（docs/06） | 低（需 Redis≥4.0 + Linux 播放面） |
| R3 | 共享 session-DB 测试执行 flake | 质量 | 中 | 低 | 测试隔离（`tmp_path`/`monkeypatch`）；已知 `test_audit_export`+`test_oss_export` 隔离跑绿 | 低 |
| R4 | 邀请码旁路 / 暴力注册 | 安全 | 中 | 中 | `TELEOPS_INVITE_CODE` + 登录限流 5/min/IP | 低 |
| R5 | 多租户越权（读写） | 安全 | 低 | 高 | `is_visible_to`/`is_writable_by` + RBAC `assert_perm` 双层；已测试 | 低 |
| R6 | LLM 成本失控 | 成本 | 中 | 中 | 日预算 `TELEOPS_BUDGET_DAILY_CNY` + fallback | 低 |
| R7 | 进程崩溃漏信号量令牌死锁 | 可靠 | 中 | 高 | 租约 TTL 自动回收（`TELEOPS_SEM_LEASE`） | 低 |
| R8 | Caddy/HTTPS 缺失致明文截获 | 安全 | 高 | 高 | 反代必须 HTTPS + 防火墙白名单（docs/10） | 中（需自签/证书） |
| R9 | Windows 无 `fork` 限制 RQ worker | 平台 | 中 | 中 | 默认线程执行器 + `SimpleWorker` 联调 | 低（生产用 Linux） |
| R10 | LLM 端点不可达，每条告警被超时拖住（流水线"假死"） | 可靠 | 中 | 高 | ADR-011 端点级熔断（fail-fast 降级 Mock + 半开探测自动恢复）；`/health` 可见 | 低（熔断期诊断走 Mock） |

---

## 4. 数据字典（Data Dictionary）

### 4.1 关系表（SQLite 默认 / Postgres 可切，`src/core/db.py`）

| 表 | 关键字段 | 说明 |
|---|---|---|
| `users` | id, username(UNIQUE), password_hash, is_admin, org_id, created_at | 账号；默认角色 `sre`，首个 admin 由 `is_admin` 派生 |
| `org_units` | id, name, parent_id, path, level, org_type | 组织树（物化路径 `path` 做子树包含判定） |
| `roles` | id, name, builtin | 内置角色：super_admin/org_admin/sre/dev/viewer |
| `role_permissions` | role_id, permission | 角色→权限多对多 |
| `user_roles` | user_id, role_id | 用户→角色 |
| `workspaces` | id, name, adapter_id, mode(auto/manual), owner_id, org_id, created_at | 业务域；公共域 `owner_id=NULL` |
| `agents` | id, workspace_id, name, kind(ops/dev), scope(JSON[]), description, is_primary, status | 域内 Agent，按业务域持久化 |
| `requirements` | id, workspace_id, status, data(JSON), created_at, updated_at | 研发 Agent 的「工具缺口」需求单 |
| `tools` | name(PK), executor, risk(low/mid/high), require_human_approval, description, workspace_id, created_at | 工具注册表；`risk=high` 走 HITL |
| `messages` | id, workspace_id, agent_id, agent_name, kind, summary, detail, ts | 作战室消息流 |
| `audit_log` | id, ts, actor, actor_id, action, workspace_id, detail, result(ok/fail), ip | 异步审计写（`audit_queue.py`） |

**权限枚举**（7 个）：`ws.view` `ws.manage` `agent.manage` `alert.ack` `tool.exec` `audit.view` `org.manage`

### 4.2 文件存储（运行时 / 配置）

| 文件 | 类型 | 是否入库 | 说明 |
|---|---|---|---|
| `data/teleops.db` | SQLite | 否(gitignore) | 主库（用户/域/Agent/工具/审计） |
| `data/approvals.json` | JSON | 否 | HITL 审批单（`LocalApprovalStore`） |
| `data/settings.json` | JSON | 否 | `require_approval` 运行时开关 |
| `data/adapters.json` | JSON | 否 | 适配器配置（按 id 索引，配置驱动切源） |
| `data/alerts.json` | JSON | 是(已提交) | 5G 演示告警样本（24 条） |
| `data/alerts.bgl_backup.json` | JSON | 是 | BGL 旧样本备份（回滚用） |
| `data/5g_dataset/` | 目录 | 否 | 真实 5G 数据集（OSS 切源用） |
| `.env` | 环境变量 | 否 | DeepSeek Key 等敏感配置 |
| `data/.server.<port>.pid` / `teleops_server.<port>.log` | 运行时 | 否 | 进程管理产物 |

### 4.3 关键环境变量（`TELEOPS_*`）

| 变量 | 默认 | 作用 |
|---|---|---|
| `TELEOPS_HOST` / `TELEOPS_PORT` | 127.0.0.1 / 8000 | 监听地址（局域网演示设 `0.0.0.0`） |
| `TELEOPS_DB_DSN` / `TELEOPS_DB_FILE` | SQLite | PG 切换点 / 测试单文件隔离 |
| `TELEOPS_STATE_STORE` | local | 状态外部化开关（redis=多副本无状态） |
| `TELEOPS_REDIS_URL` / `TELEOPS_REDIS_PROTOCOL` | — / 3 | Redis 连接；旧版 Redis 降级 RESP2 |
| `TELEOPS_STREAM_EXECUTOR` | thread | thread/queue（队列给 Linux） |
| `TELEOPS_REQUIRE_APPROVAL` | 0 | 构建期 HITL 全局闸 |
| `TELEOPS_INVITE_CODE` | 空=开放 | 注册邀请码 |
| `TELEOPS_RATE_LIMIT` / `_READ`/`_WRITE`/`_LOGIN` | on / 120/60/5 | 滑动窗口限流 |
| `TELEOPS_LLM_BASE_URL` / `_MODEL` / `_PROVIDER` / `_TRIAGE` | deepseek | LLM 端点（私有化切这里） |
| `TELEOPS_OIDC_*` | — | 单点登录（dev mock 零依赖可演示） |
| `TELEOPS_OSS_*` | — | 对象存储（mock 兜底） |
| `TELEOPS_CORS_ORIGINS` / `TELEOPS_API_TOKEN` / `TELEOPS_JWT_SECRET` | — | 跨域 / 服务令牌 / 签名密钥 |

---

## 5. 三环境策略（Dev / Staging / Prod）

> 核心约束：**数据不出域、模型可私有化**是真实环境的前置条件（ADR-006）。

| 维度 | Dev（本地/演示） | Staging（预发/验收） | Prod（生产） |
|---|---|---|---|
| 数据库 | SQLite（默认） | Postgres（`TELEOPS_DB_DSN`） | Postgres（主从） |
| 副本 | 单进程 | 多 worker（`--workers N`）+ Caddy 多后端 | 多副本 + Redis 状态外置 |
| 状态层 | local | redis（可选） | redis（`TELEOPS_STATE_STORE=redis`） |
| LLM | mock / DeepSeek | 模型私有化端点 | 模型私有化（不出域） |
| 数据 | 合成 5G 样本 | 脱敏/合成 | 真实 OSS（须满足红线） |
| 接入 | 本机 8001 前端 | Caddy HTTPS + 防火墙白名单 | Caddy HTTPS + OIDC + 邀请码 |
| 合规 | 不出域（无真实数据） | 等保二级测评 | 等保三级测评（docs/11） |
| 用途 | 开发/面试演示 | 验收/压测 | 百人团队日常 |

**环境晋升规则**：Dev 验证 → Staging 跑全量 pytest + 量化基准（Top-1/噪声抑制/修复率）→ Prod 仅在生产就绪清单（docs/10 §上线检查清单）全绿后放开真实数据源。

---

## 6. 质量门（Quality Gates）

### 6.1 当前测试资产（已落地）
- **规模**：测试套件共 **292 个测试函数**（pytest 节点约 **420**，含参数化）；本地入口 `pytest tests/ -q`。
  （v0.8.49 收尾新增 `tests/test_circuit_breaker.py` 10 例）
- **依赖外部件的用例**：约 6–12 个依赖 Redis / Postgres 的用例（如 `test_multi_replica_redis`、`test_approvals_redis_externalized`、`test_db_postgres` 真连分支）在本机未起对应服务时自动 `skip`，不阻断。
- **覆盖范围**：RBAC 接线、状态外部化（限流/任务/流/信号量）、HITL 审批、审计时间线、适配器（5G/OSS/Prometheus/Loki）、根因基准、OIDC、多租户隔离、Caddy 渲染等核心闭环均有专属测试文件。

### 6.2 合并门槛（零回归是硬指标）
- 任何改动不得引入**新的**失败；全量 `pytest tests/ -q` 须 `exit 0`。
- 语法/类型门：`python -m py_compile src/**/*.py` 全绿；前端 `node --check web/app.js` 全绿。
- **已修复的 flake（v0.8.49 收尾，当前无豁免项）**：`test_audit_export` / `test_oss_export` 曾偶发
  `sqlite3.OperationalError`。根因不是「共享 DB 天生不稳」，而是这两个用例的 `_insert_audit` **绕过
  线程安全封装**、直连共享连接在 `db._LOCK` 之外 `commit()`——异步审计写后台线程先提交了同一连接上的
  事务，测试自己的 `commit()` 就报"无活动事务"。改为走 `db.execute()`（持锁并提交）后全量跑稳定全绿。
  ⚠️ 教训：**测出来的"偶发"往往有确定根因，先查并发与锁边界，别急着归类为 flake。**

### 6.3 CI 门禁（已接线并已转绿：`.github/workflows/ci.yml`）
- **已落地**：推送 / PR 到 `main` 即触发（ubuntu-latest），五道守卫任一失败即阻断合并：
  ① **敏感文件守卫**——`.env` 类文件不得入库（排除 `*.example / *.sample / *.template` 合法示例）；
  ② **Lint**——`ruff check .`（`.ruff.toml` 只开 `E9/F63/F7/F82`，见下方分级策略）；
  ③ **编译门**——`python -m compileall src scripts tests`；
  ④ **前端语法门**——`node --check web/app.js`；
  ⑤ **测试门**——全量 `pytest tests/ -q`。
- **门禁分级策略（为什么不全开规则）**：一次性开 E/F/W 全套会让门禁首次跑红、被迫挂
  `continue-on-error`（等于门禁失效）。先守住「语法错误 / 未定义名 / 致命 bug」——本次上线即抓出
  `src/workers/stream_tasks.py` 用了 `Any` 却未导入（F821，被 `from __future__ import annotations`
  掩盖才没在运行时炸），证明关键规则集是有效的；后续再逐条收紧。
- **诚实边界（已知覆盖缺口）**：依赖真实 Redis 二进制 / Postgres DSN 的集成用例在 CI 上**仍 skip**——
  它们按 Windows 路径找 `tools/redis/redis-server.exe`。需改为环境变量注入二进制路径后，才能在
  Linux CI 真实跑通（待办，见 §7）。

---

### 6.4 CI 首次接线抓到的 4 个真问题（排障实录）

> **这段是面试最好的素材**：接 CI 之前本机全绿、我一度以为"只差把 CI 建起来"。
> 接上之后连红 3 次，每次都暴露出**本机掩盖的真问题**——这正是门禁的价值。

| # | 现象（CI 上） | 真根因 | 修复 |
|---|---|---|---|
| 1 | 6 个用例失败（`test_state_store` 4 + `test_semaphore` 2），本机全绿 | **依赖漂移**：fakeredis 需额外装 `lupa` 才有 Lua 后端；本机装过 lupa，全新环境没有 → 限流窗口与分布式信号量的 Lua 实现不生效（该拦截的被放行） | `requirements-dev.txt` 改 `fakeredis[lua]>=2.0` |
| 2 | 每次红**不同**用例、两个 Python 版本来回漂（`test_denied_op_is_audited` 等审计断言） | **异步落库竞态**：审计是后台线程写（v0.8.36），用例在动作后立刻查 `/audit`，撞上"还没落库" | 新增 `TELEOPS_AUDIT_SYNC` 同步开关，测试环境默认同步、生产保持异步；`test_audit_queue.py` 自我豁免以保留异步语义测试 |
| 3 | `assert 'ws-2' not in body` 偶发红 | **子串误命中**：业务域 id 形如 `ws-2` / `ws-20`，子串判断把 `ws-20` 误判成 `ws-2`（隔离逻辑其实没坏——同样逻辑的 `test_oss_export` 用按列比对就一直是绿的） | 导出隔离断言改 `csv.DictReader` **按列精确比对** |
| 4 | ruff 报 F821 | `src/workers/stream_tasks.py` 用了 `Any` 却未导入，被 `from __future__ import annotations` 延迟求值掩盖，运行时没炸但类型检查与未来改动会踩雷 | 补 `from typing import Any` |

**排障方法值得记一笔**：GitHub Actions 的 job 日志需认证下载（403），但**注解（annotations）可公开读取**。
于是让测试门在失败时把 `FAILED` 行以 `::error::` 抛出变成注解，无需任何凭据就拿到了精确失败用例名——
否则只能靠猜（本机是 Windows、CI 是 Linux，无法本地复现）。

**当前状态**：`py3.11` + `py3.13` 双矩阵 **均 success**（run 36158192237）。

---

## 7. 验收清单（面试照讲 / 上线核对）

- [x] 多租户硬隔离：注册建个人域 + 读/写双层闸（越权写→403、读→404）
- [x] RBAC 引擎接线：5 角色 7 权限，`assert_perm` 路由层唯一入口 + 角色运营接口（两道锁死闸）
- [x] HITL 两层闸：构建期全局闸 + 运行时按工具风险；审批态可跨副本外部化
- [x] 状态外部化：限流/任务/流/信号量四类状态经 Redis 实战验证（docs/06，7/7 通过）
- [x] 可观测：/health、/api/info、/metrics（含 replica_id）、可回放审计时间线
- [x] 合规红线：数据不出域 + 模型私有化能力（`TELEOPS_LLM_BASE_URL` 零代码切换）
- [x] 量化指标：根因 Top-1（双口径）/ 噪声抑制率 / 修复成功率（仿真）/ MTTR 显式占位（不编造）
- [x] 一键演示：5G 主题实时告警流 + 走查文档（docs/12，`python scripts/demo.py`）
- [x] 修复共享-DB 测试 flake（`_insert_audit` 改走线程安全的 `db.execute()`）
- [x] 接入 GitHub Actions CI（敏感文件 / lint / 编译 / 前端 / 测试五道守卫，py3.11+3.13 双矩阵**已转绿**）
- [x] CI 首次接线暴露的 4 个真问题全部修复（详见 §6.4：fakeredis[lua] / 审计同步 / 子串断言 / `Any` 未导入）
- [x] LLM 端点级熔断（ADR-011：三态 fail-fast + 半开探测 + /health 可观测，10 例测试）
- [ ] 让 Redis / Postgres 集成用例在 Linux CI 真实跑起来（改环境变量注入二进制路径）
- [ ] 真实 OSS 数据闭环（卡用户给导出文件 + 满足合规红线）

---

## 8. 给面试官的三句话

1. **这不是 Demo，是可演进的系统**：单进程能演示，切 `TELEOPS_STATE_STORE=redis` 就变多副本无状态，切 `TELEOPS_DB_DSN` 就上 Postgres——架构决策都有 ADR 背书。
2. **安全与合规是设计约束，不是补丁**：多租户读写双闸、RBAC、`HITL`、数据不出域红线，全部在第一次写代码时就作为硬约束落地。
3. **诚实面对边界**：MTTR 用占位不编造、CI 还没接、真实数据卡合规——我把「已知缺口」写进风险登记册和验收清单，而不是假装完成。
