# TeleOps 项目 · 求职材料包

> 用于投递三大运营商「云网运维 / 数字化研发 / 安全运营」岗位的口语化讲解素材。
> 项目代码：`TeleOps/`（Gradio 三 Tab 前台 + FastAPI 后端 + 双 Agent 闭环）。

---

## 一、一句话定位（背这版）

> "我搭了一个 **Agent 平台底座**，上面跑了两个智能体：**研发数字员工**负责造运维工具、沉淀知识，**运维 Agent** 负责告警降噪、根因推理、调工具处置。两者通过『故障反馈』闭环联动，对应运营商讲的『业务运维一体化』。整套东西零成本、可离线演示、可部署上线。"

---

## 二、技术栈（面试能讲清楚即可）

| 层 | 选型 | 说明 |
|---|---|---|
| 编排 | LangGraph | 有状态工作流，可分支/回退/可观测 |
| 模型 | DeepSeek（抽象层可切 Qwen/GLM/本地 Ollama） | 国产化叙事点 |
| 知识库 | Chroma 向量库（含字符级兜底检索） | RAG 检索增强 |
| 图数据 | networkx | CMDB 拓扑/根因爆炸半径 |
| 后端 | FastAPI | 鉴权(JWT) + 指标(/metrics) + 健康检查 + 限流 |
| 前端 | Gradio 三 Tab | 运维/研发/闭环看板 |
| 数据 | 公开 LogHub 日志 + MITRE ATT&CK + 自建模拓扑 | 真实性与差异化并存 |

---

## 三、STAR 话术（应对"介绍一个你做的项目"）

- **S（情境）**：运营商云网告警海量、根因靠老专家、工具与知识分散，运维与研发割裂。
- **T（任务）**：一个月内，白手起家做一个能体现「AI + 运维 + 研发协同」的作品集，对标三大运营商岗位要求。
- **A（行动）**：
  1. 先搭**平台底座**（LLM 抽象层 + 能力层：CMDB 图 / 工具库 / 知识库），所有 Agent 共用一套；
  2. 写**运维 Agent**（降噪→根因推理→调工具→处置）与**研发 Agent**（读反馈→自动生成工具脚本并注册→沉淀 SOP）；
  3. 用 LangGraph 把两者编排成**纵向闭环**：运维缺工具 → 研发造 → 运维复用；
  4. 套 **Gradio 三 Tab 前台 + FastAPI 后端**，并部署。
- **R（结果）**：端到端跑通闭环（实测：运维识别缺 `temperature_probe` → 研发自动生成并注册 → 运维复用成功）；零依赖可离线演示；数据用真实公开日志保证可信度。

---

## 四、三大亮点（分别对应你看到的三类 JD）

1. **对应岗位 A（云网+数智化运维）**：AIOps 根因推理结合 CMDB 拓扑 + 知识库，告警降噪与 Top-k 假设可追溯。
2. **对应岗位 B（数字化研发）**：数字员工平台化思路——工具可插拔、动态注册、风险分级、SOP 自动沉淀，体现工程治理而非调 API；并已做生产化加固（容器化部署 / SQLite 持久化 / JWT 登录 / 限流 / Prometheus+Grafana 可观测），不是只跑得起来的 Demo。
3. **对应岗位 A 安全线**：架构天然可挂「安全 Agent」（同一底座 + 安全工具/知识库），知识库已含 MITRE ATT&CK。

---

## 五、面试深挖 Q&A（提前练）

**Q1：根因推理准确吗？怎么保证？**
A：演示版用 LLM + 拓扑 + 知识库三源融合；工程上我把根因过程留 trace 可复盘，并用真实公开日志做了领域适配说明。真正落地需接入实时遥测与历史工单做评测集。

**Q2：为什么不用现成 AIOps 平台？**
A：作品集目的是体现"我理解平台怎么造"。底座抽象（LLM/工具/知识库）可替换，真实环境可对接贵司现有监控与 CMDB。

**Q3：数据哪来的？**
A：分两部分——真实公开数据（LogHub 日志、MITRE ATT&CK）保证真实性；自建模数据（电信 CMDB 拓扑、工具注册表、反馈工单）因真实运营商拓扑不公开，由我按场景建模。统一 Schema 接入，能力层零改动。

**Q4：闭环里研发自动造工具安全吗？**
A：工具库有 risk 分级，高风险需人工确认；自动生成的工具先注册为 low-risk 探针，且全部留痕在工具库与 trace，可审计可回滚。

**Q5：国产化/信创你怎么考虑？**
A：LLMClient 抽象层，换 DeepSeek/Qwen/GLM 只改配置；向量库与工具均可私有化部署，无外部强依赖。

---

## 六、1 页项目简介（可直接打印/贴 GitHub README）

```
┌──────────────────────────────────────────────┐
│  TeleOps · 运营商云网智能运维与安全 Copilot      │
├──────────────────────────────────────────────┤
│  类型：Agent 平台 + 双智能体纵向闭环（个人作品集） │
│  周期：1 个月独立开发（W1 地基→W4 部署）          │
│                                                │
│  核心能力：                                     │
│   · 运维 Agent：告警降噪 / 根因推理 / 工具调用    │
│   · 研发 Agent：自动造工具 / 注册 / SOP 沉淀      │
│   · 闭环：运维缺工具→研发造→运维复用（可观测）    │
│                                                │
│  技术：LangGraph · DeepSeek(可切国产) · Chroma   │
│        networkx · FastAPI · Gradio · 公开+自造数据│
│                                                │
│  亮点：零依赖可离线演示 · 统一 Schema 接公开数据   │
│        · LLM 抽象层支持信创替换                  │
│        · 生产化：容器化/持久化/JWT/限流/可观测     │
│                                                │
│  交付：GitHub 仓库 + 在线 Demo + 录屏 + 本文档    │
└──────────────────────────────────────────────┘
```
