# TeleOps 后端精简依赖（仅 FastAPI 真正需要的）
# 已剔除根 requirements.txt 里的 gradio / chromadb / pandas / httpx / pyyaml
# （gradio 仅旧 Gradio 原型用；chromadb 仅在 USE_CHROMA=1 时按需加载；其余未使用）
fastapi>=0.110
uvicorn>=0.27
pydantic>=2.0
python-dotenv>=1.0
openai>=1.0
# 上限必须锁 0.3：langgraph 1.x 重构了 langgraph.graph 的导出与 StateGraph 语义，
# 本项目代码基于 0.2.x API，装到 1.x 会直接 import 失败（CI 曾因此必挂）
langgraph>=0.2,<0.3
networkx>=3.0

# 可选：向量检索（知识库 RAG）。如需启用，取消注释并在 compose 中设 USE_CHROMA=1
# chromadb>=0.5
