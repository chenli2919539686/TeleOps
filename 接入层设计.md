# TeleOps 接入层（适配器）设计说明

> 本文档说明 TeleOps 如何「叠加在现有运维系统之上」而非替换它们：通过**适配器**把外部数据接入内核统一 Schema，并把 Agent 决策经执行适配器下发到设备。

## 1. 核心思想：内核只认统一 Schema

TeleOps 内核（运维 Agent / 研发 Agent / 消息栏 / 工具库）只处理四类统一结构：

| 统一结构 | 字段 | 来源 |
|---|---|---|
| `Alert` | alert_id / ts / source / metric / host / severity / value / message / tags / is_noise | 告警适配器 |
| `Tool` | name / description / approval_level / params | 研发 Agent 产出，工具库注册 |
| `CMDB` | nodes / edges（依赖关系） | CMDB 适配器 |
| `Feedback` | feedback_id / summary | 工单/消息栏适配器 |

所有外部系统（Prometheus、Zabbix、iMaster、ELK、蓝鲸、ITSM、SSH…）都**先进适配器转成统一结构，再进内核**。因此 `data/*.json` 不是"假数据"，而是"真实对接时适配器输出的标准形态"。

## 2. 适配器分类（北向 / 南向）

- **北向（感知）**：外部系统 → 内核。`AlertAdapter / LogAdapter / CMDBAdapter / TicketAdapter / KnowledgeAdapter`
- **南向（执行）**：内核 → 外部系统。`ExecAdapter`

## 3. 已落地接口清单（`src/adapters/`）

| 适配器 id | 类型 | 方向 | 状态 | 对接真实系统 | 真实接入要做的事 |
|---|---|---|---|---|---|
| `alert-prometheus` | alert | 北向 | **sample（可跑）** | Prometheus / Alertmanager | 已在 `parse_webhook` 实现标准 webhook 解析；生产只需把 Alertmanager receiver 指向 `/adapters/alert/ingest?adapter_id=alert-prometheus` |
| `cmdb-local` | cmdb | 北向 | **sample（可跑）** | 本地 topology.json | 把 `_read_topology` 换成蓝鲸/网管 API 调用 |
| `knowledge-local` | knowledge | 北向 | **sample（可跑）** | 本地 kb/ 目录 | 把 `ingest` 换成 Confluence REST 同步 |
| `exec-local` | exec | 南向 | **sample（可跑）** | 内核 ToolRegistry | 把 `execute` 换成 SSH/Netconf 下发 |
| `alert-zabbix` | alert | 北向 | **sample（可跑）** | Zabbix | 已实现 `parse_webhook`（Webhook 媒体类型报文）+ `fetch_problems`（problem.get API）；配置 `data/adapters.json` 的 base_url+令牌即切真实连接 |
| `alert-imaster` | alert | 北向 | reserved | 华为 iMaster NCE | 实现告警北向接口映射 |
| `log-elk` | log | 北向 | **sample（可跑）** | ELK / Elasticsearch | 已实现 `fetch_recent` 调 ES `_search`（query_string）；配置 `data/adapters.json` 的 base_url+index 即切真实连接 |
| `cmdb-blueking` | cmdb | 北向 | reserved | 蓝鲸 CMDB | 实现 `pull_topology` 调蓝鲸 API |
| `ticket-itsm` | ticket | 北向 | reserved | ITSM / 钉钉 / 企微 | 实现 `to_feedback` 映射工单 |
| `exec-ssh` | exec | 南向 | reserved | SSH / Netconf | 实现 `execute` 经跳板机下发（高风险走人工闸） |
| `knowledge-confluence` | knowledge | 北向 | reserved | Confluence / Runbook | 实现 `ingest` 调 Confluence REST |

> `sample` = 接口已接好且能真实运行（本地数据或标准协议）；`reserved` = 接口契约与对接说明已设计好，调用返回 `reserved` 标记，落地时只需补具体 API 调用，内核与前端零改动。

### 3.1 配置真实连接（配置驱动 + demo 兜底）

Zabbix、ELK 这类「可真实对接」的适配器采用**配置驱动**：在 `data/adapters.json`（不入库，示例见 `data/adapters.example.json`）按 adapter id 填写外部系统地址与凭据后，即自动从 demo 模式切换为真实连接；未配置时回退 demo 模式（映射逻辑与真实路径完全一致，保证演示/单测不依赖外网）。

```json
{
  "alert-zabbix": {"base_url": "http://zabbix/api", "api_token": "", "user": "Admin", "password": "", "verify_ssl": false},
  "log-elk":      {"base_url": "http://es:9200", "index": "logs-*", "api_key": "", "user": "", "password": "", "verify_ssl": false}
}
```

- Zabbix 还支持 `fetch_problems()` 直接调 `problem.get` API 拉活跃告警（需 `base_url` + `api_token` 或 `user/password`）；Webhook 路径则把 Zabbix 媒体类型指向 `POST /adapters/alert/ingest?adapter_id=alert-zabbix`。
- ELK 走标准 ES `_search`（query_string），支持 `api_key`（Bearer）或 basic auth。

## 4. 后端端点

- `GET  /adapters` —— 列出全部适配器（按 `adapter_type` 过滤）
- `POST /adapters/{id}/test` —— 连通性探测（预留接口返回待接入说明）
- `POST /adapters/alert/ingest` —— 外部告警 webhook 入口：选适配器解析 → 统一 Alert → 喂运维 Agent 根因分析
  - 鉴权：与其他写接口一致（JWT 登录 或 `X-API-Token: $TELEOPS_API_TOKEN`，后者即 Alertmanager `webhook_configs.bearer_token` 的对接方式）

### 4.1 没有真实告警系统时：`scripts/alert_simulator.py`

手头没有 Zabbix / Prometheus 时，用轻量告警生成器合成告警喂真实链路（报文是合成的，端点/解析/根因分析全是真的）：

```bash
# 登录方式（本地开发）
python scripts/alert_simulator.py --count 3 --user admin --password ******
# 服务令牌方式（设置了 TELEOPS_API_TOKEN，同 Alertmanager 模式）
python scripts/alert_simulator.py --count 3 --token $TELEOPS_API_TOKEN
# 持续模式：每 20s 一批
python scripts/alert_simulator.py --loop --interval 20 --user admin --password ******
```

输出会展示每条告警的：根因结论、已探测工具、缺失工具（可触发工具生成闭环）。

## 5. 闭环在真实环境里怎么跑

```
网管告警 ──AlertAdapter──▶ 统一Alert ──▶ 运维Agent 根因
                                        │
                          调 ExecAdapter 执行已有工具(只读/低风险)
                                        │
                          发现缺工具 ──TicketAdapter──▶ 消息栏需求
                                        │
                    (自动/手动派发) ──▶ 研发Agent 造工具 + 注册 + SOP
                                        │
                          新工具经 ExecAdapter 真正下发设备 ──▶ 闭环
```

## 6. 两个必须守住的设计点

1. **私有化部署、数据不出域**：运营商/政企强制要求。TeleOps 跑内网，适配器只做协议对接；配合 `LLMClient` 抽象层可切国产模型，正好对上**信创**叙事。
2. **高风险操作必须审批**：`reboot`/`reset` 设备不能 Agent 自作主张。`tool_registry.requires_approval()` + 消息栏人工闸 = Agent 提建议、人点确认。

## 7. 落地路径（分阶段）

- 阶段 1 感知：接 `alert-prometheus`（现已可跑）+ `cmdb-local` → Agent 能"看懂"问题
- 阶段 2 处置：把 `exec-local` 换成真实 SSH/Netconf（先只读/低风险工具）→ Agent 能"动手"
- 阶段 3 自进化：接知识库 + 让研发 Agent 基于真实 API 造工具 → 形成闭环
