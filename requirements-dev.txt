# 开发 / 测试 / CI 专用依赖（与运行时依赖分离）
# 安装：pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8.0
# D3：tests/test_state_store.py / test_semaphore.py 用 fakeredis 真实执行 Lua 脚本
# （限流滑动窗口与分布式信号量都是 Lua 原子实现），校验其与本地实现语义对齐。
#
# ⚠️ 必须带 [lua] extra：fakeredis 默认**不含 Lua 后端**，缺 lupa 时 Lua 脚本不生效。
# CI 首次跑红正是此因——本机装过 lupa 所以全绿，全新环境却挂了 6 个用例
# （state_store 4 个 + semaphore 2 个，表现为该拦截的被放行）。典型的依赖漂移。
fakeredis[lua]>=2.0
# 仅在选用 Redis 后端时才需要客户端包（src/core/state_store.py 内延迟导入，非硬依赖）：
# 设 TELEOPS_STATE_STORE=redis 前需 pip install redis
redis>=5.0
# D3 第三步：告警流走队列调度（TELEOPS_STREAM_EXECUTOR=queue）才需要 rq；
# tests/test_stream_queue.py 用 fakeredis + SimpleWorker 验证入队与真实消费
rq>=2.0
# Postgres 真连集成测试（tests/test_db_postgres.py 的 test_pg_schema_and_crud）：
# 设 TELEOPS_TEST_PG_DSN 指向可达 Postgres 时运行，否则自动 skip。psycopg3 二进制包。
psycopg[binary]>=3.1
